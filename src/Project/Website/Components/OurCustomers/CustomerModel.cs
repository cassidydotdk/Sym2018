namespace Project.Website.Components.OurCustomers
{
	// So this class would not really live here. But there is nothing else
	// prompting for building up a complete Customer Information Architecture, so for
	// now I'm just going to keep this very very simple

	public class CustomerModel
	{
		public string ImageUrl { get; set; }
		public string ImageAlt { get; set; }
	}
}